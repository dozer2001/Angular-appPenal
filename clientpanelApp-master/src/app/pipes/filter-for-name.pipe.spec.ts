import { FilterForNamePipe } from './filter-for-name.pipe';

describe('FilterForNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForNamePipe();
    expect(pipe).toBeTruthy();
  });
});
